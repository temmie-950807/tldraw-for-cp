export const SQUARE_SIZE = 100;

export const COLOR_MAP: { [key: string]: string } = {
    // 牆壁
    '#': "black",
    '@': "black",
    '%': "black",

    // 地板
    '.': "black",
};

export const FILL_MAP: { [key: string]: string } = {
    // 牆壁
    '#': "semi",
    '@': "semi",
    '%': "semi",

    // 地板
    '.': "solid",
}; 